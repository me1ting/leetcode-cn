# 简介

[原题](https://leetcode.cn/problems/is-subsequence/)

# 解析

## 双指针法

我们可以使用双指针，一个指向`s`的当前元素，一个执行`t`的查找位置。

## 动态规划

由于题目限定所有字符均为小写ascii字母。我们可以构建一个表：`dp[c][i]`，表示字符`c`在`t[i:]`中出现的首次位置。

动态规划仅适用于需要重复查找`t`的场景。